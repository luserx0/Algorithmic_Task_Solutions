#include "hiddenlib.h"

using namespace std;
int hidden(int x, int y){
    int range = compare(x,y);
    if (range == 0)
        return y/2+1;
    if (range == 1)
        hidden (x, y/2 +1)
        
    if (range == -1)
}   

int main(){
    int N = getN();

    ishidden(1);
    ishidden(N);

    int box = hidden(1, N);

    ishidden(box);
    return 0;
}
