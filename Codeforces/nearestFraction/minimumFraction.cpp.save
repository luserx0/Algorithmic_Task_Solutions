#include <stdio.h>

#define maxN 100000

using namespace std;


int main(){
    int x, y, n, a, b, i, j;
    scanf("%d %d %d", &x, &y, &n);

    if (y < n) {
        b = y;
        a = x;
    }
    else {
        
        
    }
        

    printf("%d/%d\n", a, b);

    return 0;
}
// luserx0
