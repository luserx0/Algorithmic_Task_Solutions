#include <stdio.h>
#include <algorithm>
using namespace std;


long MaxSum(){
    //Kadane's Algorithm
    int mex = 0, sum = 0;

    for(i = 0; i < n; i++){
        sum = sum + numbers[i];
        if(sum < 0)
            sum = 0;
        if(mex < sum)
            mex = sum;
        }
    return mex;
}


int main(){
    
    
    
    
    return 0;
}
